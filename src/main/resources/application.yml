spring:
  datasource:
    hikari:
      connection-timeout: 20000
    password: ''
    url: jdbc:mysql://localhost:3306/orbuslinkprod?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect